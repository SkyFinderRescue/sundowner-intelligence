name: Reconstruct Historical Sundowners

on:
  workflow_dispatch:
  push:
    branches: ["main"]
    paths:
      - "tools/reconstruct-events.js"
      - "data/known-sundowner-events.json"
      - ".github/workflows/reconstruct-history.yml"

permissions:
  contents: write

concurrency:
  group: sundowner-history-reconstruction
  cancel-in-progress: true

jobs:
  reconstruct:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: '22'
      - run: node --check tools/reconstruct-events.js
      - name: Reconstruct documented events
        id: build
        continue-on-error: true
        run: node tools/reconstruct-events.js
      - name: Preserve outputs before syncing current main
        if: always()
        env:
          OUTCOME: ${{ steps.build.outcome }}
          SHA: ${{ github.sha }}
        run: |
          mkdir -p /tmp/si-history
          node - <<'NODE'
          const fs=require('fs');
          fs.writeFileSync('/tmp/si-history/status.json',JSON.stringify({checked_at:new Date().toISOString(),source_commit:process.env.SHA,outcome:process.env.OUTCOME},null,2)+'\n');
          NODE
          if [ "$OUTCOME" = "success" ]; then cp data/historical-event-reconstructions.json /tmp/si-history/recon.json; fi
      - name: Publish reconstruction and status on current main
        if: always()
        env:
          OUTCOME: ${{ steps.build.outcome }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git fetch origin main
          git reset --hard origin/main
          mkdir -p validation data
          cp /tmp/si-history/status.json validation/history-reconstruction-status.json
          if [ "$OUTCOME" = "success" ]; then cp /tmp/si-history/recon.json data/historical-event-reconstructions.json; fi
          git add validation/history-reconstruction-status.json
          if [ "$OUTCOME" = "success" ]; then git add data/historical-event-reconstructions.json; fi
          git commit -m "Record historical Sundowner reconstruction [skip ci]" || exit 0
          git push origin HEAD:main
      - name: Fail if reconstruction failed
        if: steps.build.outcome != 'success'
        run: exit 1
